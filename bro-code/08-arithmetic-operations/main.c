#include <stdio.h>

int main()
{
    // arithmetic operations

    // + (addition)
    // - (subtraction)
    // * (multiplication)
    // / (division)
    // % (modulus)
    // ++ (increment)
    // -- (decrement)
    // += (add and assign)
    // -= (subtract and assign)
    // *= (multiply and assign)
    // /= (divide and assign)
    // %= (modulus and assign)

    int x = 5;
    int y = 2;

    // float z = x / (float)y;

    int z = x % y;

    printf("%d", z);

    return 0;
}